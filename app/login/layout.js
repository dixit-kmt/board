export const metadata = {
  title: "Login | Board.",
  description: "Login Page of the Listed Dashboard",
};

export default function RootLayout({ children }) {
  return (
    <>
      <div>{children}</div>
    </>
  );
}
